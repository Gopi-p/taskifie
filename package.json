{
  "name": "taskifie",
  "version": "1.0.0",
  "description": "A flutter web app to manage the tasks efficiently.",
  "main": "lib/main.dart",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:web": "flutter build web --web-renderer html && npm run cpy",
    "cpy": "cpy \"build/web/**/*\" \"deploy/web\""
  },
  "author": "Gopinath P",
  "license": "ISC",
  "dependencies": {
    "cpy-cli": "^5.0.0"
  }
}
