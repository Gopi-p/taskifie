import 'package:flutter/material.dart';

final lightThemeData = ThemeData(
  fontFamily: 'Poppins',
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue,),
);

final darkThemeData = ThemeData(fontFamily: 'Poppins');
